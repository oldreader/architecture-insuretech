type Query {
    clients(id: ID!): Client
    documents(clientId: ID!): [Document]
    relatives(clientId: ID!): [Relative]

}

type Client {
    id: ID!
    name: String
    age: Int
}

type Document {
    id: String
    type: String
    number: String
    issueDate: String
    expiryDate: String
}

type Relative {
    id: String
    relationType: String
    name: String
    age: Int
}